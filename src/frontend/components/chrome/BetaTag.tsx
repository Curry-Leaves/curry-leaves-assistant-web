/** Small "beta" pill shown next to a page title while the feature is still settling. */
export function BetaTag() {
  return (
    <span className="text-[9px] font-600 uppercase tracking-wider text-accent-dark bg-accent-soft/40 border border-accent/30 rounded-full px-1.5 py-0.5 leading-none flex-none">
      beta
    </span>
  );
}
